FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
# Source code is mounted as a volume at runtime
EXPOSE 3100
CMD ["npx", "next", "dev", "--port", "3100", "--hostname", "0.0.0.0"]
