FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
# Source code is mounted as a volume at runtime
EXPOSE 3200
CMD ["npx", "next", "dev", "--port", "3200", "--hostname", "0.0.0.0"]
